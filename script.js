var choice = prompt("welcome to AREA CALCULTOR \n ENTER YOUR CHOICE \n1.Area of rectangle \n2.Area of triangle \n3.Area of circle \n4.Area of paralellogram");
if (choice == '1' ) {

    var l = prompt ('Enter the length');
    var b = prompt ('Enter the breadth');
    var result = Number(l) *  Number(b);
    alert('the area of rectangle is' + result);
}

else if (choice == '2') {
   var h = prompt ('enter the height');
   var b = prompt ('ente the base');
   var result = number(h) * number(b);
   alert()

  

}
    